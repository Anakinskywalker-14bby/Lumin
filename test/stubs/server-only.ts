// Test stub. The real `server-only` package throws at import time when it is
// pulled into a client bundle; that guard is a build-time concern, not a
// runtime one, so tests alias it away (see vitest.config.ts).
export {};
