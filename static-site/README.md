# How To Restart A Dead Chat With Her — static export

This folder contains a simple static HTML/CSS export of the transcriptions you provided, plus a machine-readable `data.json`.

Files:
- `index.html` — human-readable gallery of the transcriptions.
- `styles.css` — styling for layout and readability.
- `data.json` — verbatim transcriptions for programmatic use.

Open `index.html` in a browser to view.
